#include "pch.h"

#include "math/Vector.hpp"

using namespace math;

TEST(VectorUnitTest, Initialization)
{
	Vector<f32, 2> vecReal2 = Vector<f32, 2>();
	//EXPECT_EQ(vecReal2.x(), 0);
	//EXPECT_EQ(vecReal2.y(), 0);
	//vecReal2 = { 0, 1 };
}